import { membersPageContent } from "./page-content";
import { sectionsByKey } from "./sections";

export const membersPageHero = sectionsByKey.members.hero;

export const membersSections = {
  directory: membersPageContent.directory,
  join: membersPageContent.join
};

export const joinCards = membersPageContent.joinCards;
